# creating tuples using ()
tuples =(1, 2, 4, 6) 
# t1 = ()  => Empty tuple
# t1 = (1) => wrong way to declare a tuple with single element
t1 = (1,)

#printing the elements of a tuple
print(tuples[0])

# cannot update tuples value
tuples[0] = 34 # tuples doesnot support item assignment